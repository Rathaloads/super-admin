export enum eBookType {
    Family = "family",
    Personal = "personal",
    Business = "business",
}

// 账单类型
export enum eBookRecordType {
    Income = 1, // 收入
    Expense = 2, // 支出
    Transfer = 3, // 转账
}