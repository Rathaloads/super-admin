---
description: 前端页面视觉设计规范（亚克力玻璃 + Fluent 动效），编写 client 页面时必须遵循
globs: client/**/*.{vue,scss}
alwaysApply: false
---

# 前端 UI 设计规范（Acrylic Fluent）

编写 `client/` 下页面时，**必须**遵循此风格。参考实现：`client/src/views/home/HomeView.vue`，共享样式：`client/src/styles/glass.scss`。

## 视觉核心

- **背景**：浅色多层渐变（`#f8fafc → #eef2ff → #f0f9ff → #faf5ff`）+ 模糊光斑（`filter: blur(60px)`，低透明度 accent 色）
- **卡片**：亚克力玻璃 `.glass-card` — 半透明白底、`backdrop-filter: blur(20px) saturate(1.4)`、内高光描边
- **圆角**：页面容器 16px，卡片/输入框/按钮 10–16px
- **轮廓**：`border: 1px solid rgba(255,255,255,0.85)` + 多层轻阴影；卡片顶部可用 accent 渐变线

## 色彩

| 用途 | 色值 |
|------|------|
| 主文字 | `#1e293b` |
| 次要文字 | `#64748b` / `#94a3b8` |
| 主强调色 | `#6366f1`（靛蓝） |
| 成功 | `#10b981` |
| 警告 | `#f59e0b` |

每张数据卡片可设独立 `--accent` CSS 变量。

## 动效（Fluent）

```scss
$fluent-ease: cubic-bezier(0.33, 0, 0.67, 1);
$duration-fast: 150ms;    // 按钮、边框
$duration-normal: 220ms;  // 卡片 hover
$duration-slow: 280ms;    // 进度条宽度
```

- **卡片 hover**：`translateY(-2px)` + 阴影加深，**禁止**弹跳/大幅位移
- **按钮 hover**：颜色加深 + 阴影增强，不移动
- **进度条**：高度 4px，渐变填充，`width` 过渡 280–300ms

## 布局

- 统计/功能区块用 **卡片网格**（`grid`，gap 16px，响应式断列）
- 优先自定义玻璃卡片，**避免**直接用 `el-card` 破坏视觉一致性
- Element Plus 仅用于图标、表格等复杂交互组件

## 禁止

- 厚重拟物、高饱和纯色大块背景、夸张 spring 动画
- 与玻璃风格冲突的默认 Element 卡片样式
